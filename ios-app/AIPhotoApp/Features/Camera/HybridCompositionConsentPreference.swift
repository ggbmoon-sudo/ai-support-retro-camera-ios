enum HybridCompositionConsentPreference {
    // The version is intentionally part of the key. A future disclosure change
    // must use a new key so an old acceptance cannot silently cover new terms.
    static let defaultsKey = "camera.live-ai-consent.2026-07-20.phase25.live-keyframes.v1"
}
