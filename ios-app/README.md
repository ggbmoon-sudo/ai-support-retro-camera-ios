# iOS App Placeholder

This folder will contain the Swift + SwiftUI iOS app.

Planned stack:

- Swift
- SwiftUI
- AVFoundation
- PhotosPicker / PHPicker
- Core Image
- Vision
- Firebase Apple SDK
- StoreKit 2

Phase 00 only creates this placeholder.

Do not add real `GoogleService-Info.plist` to git.

The real SwiftUI project should be initialized in Phase 01 or when explicitly requested.
