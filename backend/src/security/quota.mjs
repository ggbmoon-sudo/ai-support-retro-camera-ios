export function checkDevQuota() {
  // Phase 17C-Prep placeholder only. Production quota requires auth, entitlement,
  // a durable store, and cost controls; do not add those in this phase.
  return { ok: true };
}
